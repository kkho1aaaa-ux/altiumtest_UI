SELECT 
    part_number,
    value_display,
    tolerance_percent,
    package,
    temp_min_c,
    temp_max_c,
    power_rating_w
FROM components
WHERE 
    part_number ILIKE '%' || COALESCE({{searchInput.text}}, '') || '%';