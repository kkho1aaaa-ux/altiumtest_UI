SELECT
  *
FROM
  public.components
WHERE
  "part_number" ilike '%{{componentsTable.searchText}}%'
ORDER BY
  "{{componentsTable.sortOrder.column || 'id'}}" {{componentsTable.sortOrder.order !== "desc" ? "" : "DESC"}}
LIMIT
  {{componentsTable.pageSize}}
OFFSET
  {{componentsTable.pageOffset}}